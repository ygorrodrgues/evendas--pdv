application.title=PDV-gui
application.vendor=wilbert
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/PDV-gui.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.credito.jar=../PDV-core/lib/credito.jar
file.reference.estoque.jar=../PDV-core/lib/estoque.jar
includes=**
jar.compress=false
javac.classpath=\
    ${libs.swing-layout.classpath}:\
    ${reference.PDV-core.jar}:\
    ${file.reference.credito.jar}:\
    ${reference.DominioEVendas.jar}:\
    ${file.reference.estoque.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.swing-layout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swing-layout.classpath=../../../../../Program Files/NetBeans 6.0/platform7/modules/ext/swing-layout-1.0.3.jar
main.class=br.cefetrn.datinf.pdv.visao.Main
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.Dominio=../Dominio
project.DominioEVendas=../Dominio
project.PDV-core=../PDV-core
reference.Dominio.jar=${project.Dominio}/dist/Dominio.jar
reference.DominioEVendas.jar=${project.DominioEVendas}/dist/DominioEVendas.jar
reference.PDV-core.jar=${project.PDV-core}/dist/PDV-core.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
